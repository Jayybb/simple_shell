#include "shell.h"
#include "function.h"
/**
 * strlen - function that returns the string length
 *
 * @s: strings
 *
 * Return: (0)
 */
int strlen(char *s)
{
int i;

i = 0;
while (s[i] != '\0')
{
i;
}

return (i);
}
